# XYVOO HMS documentation

Place product artefacts here for version control and onboarding.

## Front Desk PRD

Add **`XYVOO_HMS_FrontDesk_PRD_v1.0.pdf`** to this folder (`xyvoo-next/docs/`) so the canonical PRD stays next to the implementation. The UI shell in `src/lib/hms/frontdesk-capabilities.ts` was derived from PRD v1; update that registry when the PDF changes.
